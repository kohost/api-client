/* @ts-nocheck */
/* This file is automatically generated. Do not modify it manually. */

import { Entity } from "./entity";
import { validateDepartment as validate } from "../validate";

/**
 * @typedef {Object} DepartmentData A division of an organization.
 * @property {string} id - Identifier of the object.
 * @property {"department"} type - Default: "department"
 * @property {string} name
 * @property {string} [description]
 * @property {(string|object)} [createdAt]
 * @property {(string|object)} [updatedAt]
 * @property {(string|object)} [deletedAt]
 */

/**
 * A division of an organization.
 * @class Department
 * @extends {Entity}
 */
export class Department extends Entity {
  /**
   * @constructor
   * @param {DepartmentData} data - The data to initialize the Department with
   */
  constructor(data) {
    super(data);
    this.id = data.id;
    this.type = data.type;
    this.name = data.name;
    if (data.description !== undefined) this.description = data.description;
    if (data.createdAt !== undefined) this.createdAt = data.createdAt;
    if (data.updatedAt !== undefined) this.updatedAt = data.updatedAt;
    if (data.deletedAt !== undefined) this.deletedAt = data.deletedAt;
  }
}

Object.defineProperty(Department.prototype, "schema", {
  value: {
    $schema: "http://json-schema.org/draft-07/schema",
    $id: "department.json",
    title: "Department",
    description: "A division of an organization.",
    type: "object",
    required: ["id", "type", "name"],
    additionalProperties: false,
    properties: {
      id: { $ref: "definitions.json#/definitions/id" },
      type: { type: "string", enum: ["department"], default: "department" },
      name: { type: "string" },
      description: { type: "string" },
      createdAt: { $ref: "definitions.json#/definitions/date" },
      updatedAt: { $ref: "definitions.json#/definitions/date" },
      deletedAt: { $ref: "definitions.json#/definitions/date" },
    },
  },
});

Object.defineProperty(Department.prototype, "validator", {
  get: function () {
    return validate;
  },
});
